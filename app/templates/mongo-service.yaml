apiVersion: v1
kind: Service
metadata:
  name: death-metal-mongo-service
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app.kubernetes.io/instance: mongodb
  ports:
  - port: 27017
    targetPort: 27017
