apiVersion: v1
kind: Service
metadata:
  name: death-metal-web-service
  namespace: death-metal
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: death-metal-web
  ports:
  - port: 8080
    targetPort: 80
